{
  "name": "databricks-graph-backend",
  "version": "1.0.0",
  "description": "Backend API for Databricks SQL Warehouse integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node db/seed.js",
    "reseed": "node db/seed.js",
    "seed-if-empty": "node -e \"const db = require('./db/database.js').initDatabase(); if (require('./db/database.js').isDatabaseEmpty(db)) { db.close(); require('./db/seed.js').seedDatabase(); } else { db.close(); console.log('Database already populated'); }\"",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "keywords": [
    "databricks",
    "graph",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@databricks/sql": "^1.8.0",
    "better-sqlite3": "^9.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^7.0.0"
  }
}
